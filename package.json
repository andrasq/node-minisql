{
  "name": "mysqule",
  "version": "0.10.5",
  "description": "small, quick mysql database bindings",
  "license": "Apache-2.0",
  "main": "lib/minisql",
  "files": ["README.md", "lib", "test"],
  "engines": { "node": ">= 0.7" },

  "author": "andrasq",
  "readme": "see README.md in the repo",
  "repository": { "type": "git", "url": "git://github.com/andrasq/node-minisql" },

  "dependencies": {
    "q-utf8": "0.1.3",
    "qlist": "0.14.0"
  },
  "devDependencies": {
    "qmock": "0.16.1"
  },

  "scripts": {
    "NOTE re testing: need qnit and a mysql database.  See .travis.yml in the repo": "",
    "test": "qnit test",
    "coverage": "nyc -r text -r lcov qnit -t 10000 test",
    "clean": "rm -rf coverage .nyc_output"
  }
}
